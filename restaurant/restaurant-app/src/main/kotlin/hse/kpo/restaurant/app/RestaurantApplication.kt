package hse.kpo.restaurant.app

import org.springframework.boot.SpringApplication

class RestaurantApplication {
    fun main(args: Array<String>) {
        SpringApplication.run(RestaurantApplication::class.java, *args)
    }
}